var Index = {

   /* Gravar: function () {
        var dados = {
            login: fd.getById("login"),
            senha: fd.getById("senha")

        }
        if (dados.login.trim() == "" || dados.senha.trim() == "") {
            alert("usuario errado!");
            return
        }
        fd.ajax("POST", "Usuario\Logar", dados, function (retornServ) {

            if (retornServ.Verificar)
        },
            function () {
                alert("erro");
            })

        
    },*/
    click: function () {
        fd.onClick()
    },
    pegarClik: function () {
        alert("a");
        //senha: fd.getById("A");
        //fd.getValById("senha");
    }

}